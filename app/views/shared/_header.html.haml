%nav.navbar.navbar-default.navbar-fixed-top
  .container-fluid
    %a.navbar-brand{:href => "#"} Iolite
    %ul.nav.navbar-nav.navbar-right
      - if logged_in?
        %li= link_to @current_user.name, "#"
        %li= link_to "Sign Out", sign_out_path
      -else
        %li= link_to "Sign In", "/auth/google_oauth2"
